# 03 · 阶段 1：灰盒原型实施计划（6/12~13）

> **执行说明（给 AI 编辑器）：** 按任务顺序执行，一次一个任务。每个任务内的文件路径、节点名、函数名是后续任务的依赖，**不得擅自改名**。每个任务以"验证"步骤收尾，验证通过后按给出的信息 git commit。本项目是游戏，验证方式为"运行并观察"，预期现象已写明。

**目标：** 用纯色块占位素材，把游戏六大系统（移动、调查、对话、线索笔记、剧情 flag、昼夜切换）和完整流程（开场字卡→醒来进村→五地点探索→夜幕→祠堂结局）全部跑通。本阶段结束时游戏可从头玩到尾。

**架构：** Godot 4.4 / GDScript。三个 autoload 单例：`GameState`（剧情状态）、`Router`（场景切换）、`Ui`（对话框/提示/笔记，CanvasLayer 场景）。村庄外景与 5 个室内是独立场景，玩家场景被各地图实例化，门用 Area2D 触发切换。所有占位图形用 `Polygon2D` 纯色块。

**工程位置：** 仓库根目录下的 `game/` 文件夹。

---

## 任务 0：创建 Godot 工程

**操作（这一步由人在 Godot 里做，不是 AI 编辑器）：**

- [ ] 打开 Godot 4.4 → Project Manager → Create New Project
- [ ] Project Path 选仓库根目录下新建的 `game/` 文件夹（即 `/Users/xulei/.dev/Lingguan_Nightward/game`）
- [ ] Renderer 选 **Compatibility**（兼容模式，Web 导出必须用它）
- [ ] 点 Create & Edit，编辑器打开后直接关闭即可

**验证：** `game/project.godot` 文件存在。

**提交：**

```bash
git add game/ && git commit -m "chore: 创建 Godot 4.4 工程（Compatibility 渲染器）"
```

---

## 任务 1：工程基础配置（窗口、像素渲染、输入映射、目录）

**文件：**
- 修改：`game/project.godot`
- 创建：`game/.gitignore`
- 创建目录：`game/scenes/`、`game/scenes/interiors/`、`game/scripts/`、`game/assets/`

- [ ] **步骤 1：** 创建 `game/.gitignore`，内容：

```gitignore
.godot/
*.import
```

- [ ] **步骤 2：** 用下面内容**整体替换** `game/project.godot`（若 Godot 编辑器开着，改完后它会提示重载，选重载）：

```ini
; Engine configuration file.
config_version=5

[application]

config/name="灵棺夜行·第一夜"
config/features=PackedStringArray("4.4", "GL Compatibility")

[display]

window/size/viewport_width=640
window/size/viewport_height=360
window/size/window_width_override=1280
window/size/window_height_override=720
window/stretch/mode="canvas_items"
window/stretch/aspect="keep"

[input]

move_left={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":65,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194319,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
move_right={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":68,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194321,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
move_up={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":87,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194320,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
move_down={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":83,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194322,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
interact={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":69,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
, Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":32,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}
journal={
"deadzone": 0.2,
"events": [Object(InputEventKey,"resource_local_to_scene":false,"resource_name":"","device":-1,"window_id":0,"alt_pressed":false,"shift_pressed":false,"ctrl_pressed":false,"meta_pressed":false,"pressed":false,"keycode":0,"physical_keycode":4194306,"key_label":0,"unicode":0,"location":0,"echo":false,"script":null)
]
}

[rendering]

renderer/rendering_method="gl_compatibility"
renderer/rendering_method.mobile="gl_compatibility"
textures/canvas_textures/default_texture_filter=0
```

（输入映射：WASD/方向键移动，E/空格 = `interact` 调查，Tab = `journal` 笔记。`default_texture_filter=0` 是像素游戏必需的"最近邻"采样，防止贴图发糊。）

- [ ] **步骤 3：** 创建空目录 `game/scenes/`、`game/scenes/interiors/`、`game/scripts/`、`game/assets/`（可各放一个 `.gitkeep`）

- [ ] **验证：** 打开 Godot → Project → Project Settings → Input Map，能看到 `move_left` 等 7 个动作；窗口设置为 640×360。无报错。

- [ ] **提交：**

```bash
git add game/ && git commit -m "chore: 工程基础配置（窗口/像素渲染/输入映射）"
```

---

## 任务 2：全局状态 GameState 与场景路由 Router

**文件：**
- 创建：`game/scripts/game_state.gd`
- 创建：`game/scripts/router.gd`
- 修改：`game/project.godot`（注册 autoload）

- [ ] **步骤 1：** 创建 `game/scripts/game_state.gd`：

```gdscript
extends Node
## 全局游戏状态：剧情阶段、线索、flag。autoload 单例，名为 GameState。

signal clue_added(clue_id: String)
signal phase_changed(new_phase: Phase)

enum Phase { DAY, NIGHT, ENDING }

const TOTAL_CLUES := 5

## 线索 id → 笔记界面显示文本（文案来源：docs/07-剧情文案.md）
const CLUE_TEXTS := {
	"home_letter": "家里的饭桌还摆着没动过的饭菜，爹娘留下一封没写完的信。",
	"chen_shroud": "师傅家那件寿衣的针脚里缝着符纸——师傅的死不是意外。",
	"zhang_wedding": "张老汉家供着一张阴婚帖，落款日期，是我下葬那天。",
	"yizhuang_coffin": "义庄的棺材全空了，镇尸符早已过期发黑。",
	"citang_roster": "祠堂的全村名册上，三百多个名字被红笔一个个圈掉。",
}

var phase: Phase = Phase.DAY
var clues: Array[String] = []
var flags: Dictionary = {}

func add_clue(clue_id: String) -> void:
	if clue_id in clues:
		return
	clues.append(clue_id)
	clue_added.emit(clue_id)
	if clues.size() >= TOTAL_CLUES and phase == Phase.DAY:
		set_phase(Phase.NIGHT)

func set_phase(p: Phase) -> void:
	phase = p
	phase_changed.emit(p)

func set_flag(key: String) -> void:
	flags[key] = true

func has_flag(key: String) -> bool:
	return flags.get(key, false)

## 通关后回到开头时重置全部状态
func reset() -> void:
	phase = Phase.DAY
	clues.clear()
	flags.clear()
```

- [ ] **步骤 2：** 创建 `game/scripts/router.gd`：

```gdscript
extends Node
## 场景切换：记录目标出生点名，由目标场景在 _ready 里读取并摆放玩家。

var spawn_name: String = "default"

func change_scene(scene_path: String, spawn: String = "default") -> void:
	spawn_name = spawn
	get_tree().change_scene_to_file.call_deferred(scene_path)
```

（用 `call_deferred` 是因为切场景常由碰撞回调触发，物理回调中直接切场景会报错。）

- [ ] **步骤 3：** 在 `game/project.godot` 的 `[application]` 段之后插入：

```ini
[autoload]

GameState="*res://scripts/game_state.gd"
Router="*res://scripts/router.gd"
```

- [ ] **验证：** Godot 重载工程后无任何报错；Project Settings → Globals(Autoload) 里能看到 GameState 和 Router。

- [ ] **提交：**

```bash
git add game/ && git commit -m "feat: GameState 与 Router 全局单例"
```

---

## 任务 3：玩家移动 + 灰盒村庄外景

**文件：**
- 创建：`game/scripts/player.gd`、`game/scenes/player.tscn`
- 创建：`game/scripts/village.gd`、`game/scenes/village.tscn`
- 修改：`game/project.godot`（设主场景）

- [ ] **步骤 1：** 创建 `game/scripts/player.gd`：

```gdscript
extends CharacterBody2D
## 玩家：四方向移动 + 调查交互（交互对象为 group "interactable" 中最近的 Area2D）。

const SPEED := 110.0

var can_move := true

var _nearby: Array[Area2D] = []

@onready var interact_area: Area2D = $InteractArea

func _ready() -> void:
	add_to_group("player")
	interact_area.area_entered.connect(_on_area_entered)
	interact_area.area_exited.connect(_on_area_exited)

func _physics_process(_delta: float) -> void:
	if not can_move or Ui.is_dialogue_open():
		velocity = Vector2.ZERO
	else:
		velocity = Input.get_vector("move_left", "move_right", "move_up", "move_down") * SPEED
	move_and_slide()

func _unhandled_input(event: InputEvent) -> void:
	if not event.is_action_pressed("interact"):
		return
	if not can_move or Ui.is_dialogue_open() or _nearby.is_empty():
		return
	var target := _nearest()
	if target != null and target.has_method("interact"):
		target.interact()

func _nearest() -> Area2D:
	var best: Area2D = null
	var best_d := INF
	for a in _nearby:
		var d := global_position.distance_squared_to(a.global_position)
		if d < best_d:
			best_d = d
			best = a
	return best

func _on_area_entered(area: Area2D) -> void:
	if area.is_in_group("interactable"):
		_nearby.append(area)
		Ui.show_prompt("E 调查")

func _on_area_exited(area: Area2D) -> void:
	_nearby.erase(area)
	if _nearby.is_empty():
		Ui.hide_prompt()
```

注意：本脚本引用了 `Ui`（任务 4 才创建）。**任务 3 验证时先把脚本里所有含 `Ui.` 的行临时注释掉**（共 4 处：`_physics_process` 的条件改为 `if not can_move:`，`_unhandled_input` 里删去 `Ui.is_dialogue_open()` 条件，两个 area 回调里的提示行注释），任务 4 完成后按本任务步骤 1 的原文恢复。

- [ ] **步骤 2：** 创建场景 `game/scenes/player.tscn`，节点树如下（类型写在括号里，属性写在缩进行）：

```
Player (CharacterBody2D)  ← 挂脚本 res://scripts/player.gd
├─ Body (Polygon2D)
│     polygon = [(-8,-12), (8,-12), (8,12), (-8,12)]，color = 白色 (1,1,1,1)
├─ Shape (CollisionShape2D)
│     shape = RectangleShape2D，size = (14,16)；position = (0,4)   # 碰撞盒在脚部
├─ InteractArea (Area2D)
│     monitorable = false
│  └─ IShape (CollisionShape2D)
│        shape = CircleShape2D，radius = 26
└─ Camera (Camera2D)
      position_smoothing_enabled = true
      limit_left = 0, limit_top = 0, limit_right = 1280, limit_bottom = 960
```

- [ ] **步骤 3：** 创建 `game/scripts/village.gd`（夜晚过场本任务先留空壳，任务 9 填充）：

```gdscript
extends Node2D
## 村庄外景：摆放玩家、昼夜色调、夜幕过场。

const DAY_COLOR := Color("9b95ad")    # 阴沉的白天
const NIGHT_COLOR := Color("3a3354")  # 夜晚

@onready var player: CharacterBody2D = $Player
@onready var tint: CanvasModulate = $Tint
@onready var spawn_points: Node2D = $SpawnPoints

func _ready() -> void:
	_place_player()
	tint.color = NIGHT_COLOR if GameState.phase == GameState.Phase.NIGHT else DAY_COLOR

func _place_player() -> void:
	var point: Node2D = spawn_points.get_node_or_null(NodePath(Router.spawn_name))
	if point == null:
		point = spawn_points.get_node("default")
	player.global_position = point.global_position
```

- [ ] **步骤 4：** 创建场景 `game/scenes/village.tscn`。世界尺寸 1280×960。节点树：

```
Village (Node2D)  ← 挂脚本 res://scripts/village.gd
├─ Tint (CanvasModulate)            color 随意，运行时由脚本设置
├─ Ground (Polygon2D)               polygon = [(0,0),(1280,0),(1280,960),(0,960)]，color = #4a4a42
├─ Walls (StaticBody2D)             # 地图四周边界，4 个 CollisionShape2D
│  ├─ N (CollisionShape2D)  shape=RectangleShape2D size=(1280,20)  position=(640,10)
│  ├─ S (CollisionShape2D)  shape=RectangleShape2D size=(1280,20)  position=(640,950)
│  ├─ W (CollisionShape2D)  shape=RectangleShape2D size=(20,960)   position=(10,480)
│  └─ E (CollisionShape2D)  shape=RectangleShape2D size=(20,960)   position=(1270,480)
├─ Houses (Node2D)                  # 5 栋房子，每栋 = 色块 + 碰撞 + 名字标签
│  │   # 每栋结构相同，以"自己家"为例；其余 4 栋改 position 与文本
│  ├─ HomeBlock (StaticBody2D)      position = (240, 380)
│  │  ├─ V (Polygon2D)   polygon=[(-60,-45),(60,-45),(60,45),(-60,45)]  color=#6b5b4a
│  │  ├─ C (CollisionShape2D)  shape=RectangleShape2D size=(120,90)
│  │  └─ L (Label)  text="自己家"  position=(-24,-10)
│  ├─ ChenBlock (StaticBody2D)      position = (520, 300)   L.text="陈坚家"
│  ├─ ZhangBlock (StaticBody2D)     position = (840, 380)   L.text="张老汉家"
│  ├─ YizhuangBlock (StaticBody2D)  position = (1060, 540)  L.text="义庄"
│  └─ CitangBlock (StaticBody2D)    position = (640, 140)   L.text="祠堂"
├─ SpawnPoints (Node2D)             # 全部是 Marker2D
│  ├─ default (Marker2D)        position = (640, 750)
│  ├─ wake_up (Marker2D)        position = (640, 880)   # 棺材地土坑
│  ├─ from_home (Marker2D)      position = (240, 470)
│  ├─ from_chen (Marker2D)      position = (520, 390)
│  ├─ from_zhang (Marker2D)     position = (840, 470)
│  ├─ from_yizhuang (Marker2D)  position = (1060, 630)
│  └─ from_citang (Marker2D)    position = (640, 230)
└─ Player (player.tscn 实例)
```

- [ ] **步骤 5：** `game/project.godot` 的 `[application]` 段加入主场景：

```ini
run/main_scene="res://scenes/village.tscn"
```

- [ ] **验证：** Godot 按 F5（⌘R）运行：能看到灰绿色地面、5 个棕色房块和名字、白色方块玩家出现在村中；WASD/方向键移动流畅，撞房子和地图边缘会被挡住；摄像机跟随且不出地图边界。

- [ ] **提交：**

```bash
git add game/ && git commit -m "feat: 玩家四方向移动与灰盒村庄外景"
```

---

## 任务 4：全局 UI——对话框（打字机）、交互提示、线索笔记

**文件：**
- 创建：`game/scripts/ui.gd`、`game/scenes/ui.tscn`
- 修改：`game/project.godot`（注册 Ui autoload）
- 修改：`game/scripts/player.gd`（恢复任务 3 注释掉的 `Ui.` 调用）

- [ ] **步骤 1：** 创建 `game/scripts/ui.gd`：

```gdscript
extends CanvasLayer
## 全局 UI：对话框（打字机效果）、交互提示、线索笔记。autoload 场景，名为 Ui。

signal dialogue_finished

const CHARS_PER_SEC := 25.0

@onready var prompt: Label = $Prompt
@onready var dialogue_panel: PanelContainer = $DialoguePanel
@onready var dialogue_label: RichTextLabel = $DialoguePanel/Margin/DialogueLabel
@onready var journal_panel: PanelContainer = $JournalPanel
@onready var journal_entries: VBoxContainer = $JournalPanel/JMargin/JList/JEntries

var _lines: Array = []
var _line_index := 0
var _typing := false
var _tween: Tween

func _ready() -> void:
	prompt.hide()
	dialogue_panel.hide()
	journal_panel.hide()

func is_dialogue_open() -> bool:
	return dialogue_panel.visible

## 显示一段旁白/对话。lines 为字符串数组，按 E/空格逐条推进。
func show_dialogue(lines: Array) -> void:
	if lines.is_empty() or is_dialogue_open():
		return
	_lines = lines
	_line_index = 0
	prompt.hide()
	dialogue_panel.show()
	_show_current_line()

func show_prompt(text: String) -> void:
	if is_dialogue_open():
		return
	prompt.text = text
	prompt.show()

func hide_prompt() -> void:
	prompt.hide()

func _show_current_line() -> void:
	var text: String = str(_lines[_line_index])
	dialogue_label.text = text
	dialogue_label.visible_characters = 0
	_typing = true
	_tween = create_tween()
	_tween.tween_property(dialogue_label, "visible_characters", text.length(),
			text.length() / CHARS_PER_SEC)
	_tween.finished.connect(func() -> void: _typing = false)

func _unhandled_input(event: InputEvent) -> void:
	if event.is_action_pressed("journal") and not is_dialogue_open():
		_toggle_journal()
		get_viewport().set_input_as_handled()
		return
	if not is_dialogue_open():
		return
	if event.is_action_pressed("interact") or event.is_action_pressed("ui_accept"):
		get_viewport().set_input_as_handled()
		if _typing:
			_tween.kill()
			dialogue_label.visible_characters = -1
			_typing = false
		elif _line_index < _lines.size() - 1:
			_line_index += 1
			_show_current_line()
		else:
			dialogue_panel.hide()
			dialogue_finished.emit()

func _toggle_journal() -> void:
	if journal_panel.visible:
		journal_panel.hide()
		return
	for child in journal_entries.get_children():
		child.queue_free()
	if GameState.clues.is_empty():
		var empty := Label.new()
		empty.text = "（还没有发现任何线索）"
		journal_entries.add_child(empty)
	for clue_id in GameState.clues:
		var entry := Label.new()
		entry.text = "· " + str(GameState.CLUE_TEXTS.get(clue_id, clue_id))
		entry.autowrap_mode = TextServer.AUTOWRAP_WORD_SMART
		entry.custom_minimum_size = Vector2(440, 0)
		journal_entries.add_child(entry)
	journal_panel.show()
```

- [ ] **步骤 2：** 创建场景 `game/scenes/ui.tscn`，根节点 CanvasLayer：

```
Ui (CanvasLayer)  ← 挂脚本 res://scripts/ui.gd
├─ Prompt (Label)
│     text = "E 调查"；水平居中、靠屏幕下方：anchor 预设 Center Bottom，
│     position 约 (320-宽/2, 300)，horizontal_alignment = Center
├─ DialoguePanel (PanelContainer)
│     锚到底部：offset_left=20, offset_right=620, offset_top=270, offset_bottom=350
│  └─ Margin (MarginContainer)   四边 margin = 12
│     └─ DialogueLabel (RichTextLabel)
│           fit_content = true，custom_minimum_size = (556, 44)
└─ JournalPanel (PanelContainer)
      居中：offset_left=80, offset_right=560, offset_top=40, offset_bottom=320
   └─ JMargin (MarginContainer)   四边 margin = 16
      └─ JList (VBoxContainer)
         ├─ JTitle (Label)   text = "线索 · 笔记（Tab 关闭）"
         └─ JEntries (VBoxContainer)
```

- [ ] **步骤 3：** `game/project.godot` 的 `[autoload]` 段在 Router 之后加一行：

```ini
Ui="*res://scenes/ui.tscn"
```

- [ ] **步骤 4：** 恢复 `player.gd` 中任务 3 注释掉的 `Ui.` 调用，恢复成本计划任务 3 步骤 1 的原貌。

- [ ] **步骤 5：** 临时验证代码——在 `village.gd` 的 `_ready()` 末尾加一行：

```gdscript
	Ui.show_dialogue(["这是一条测试旁白。", "按 E 推进，再按一次关闭。"])
```

- [ ] **验证：** F5 运行：底部弹出对话框，文字逐字浮现；打字中按 E 立刻显示全句；句尾按 E 进下一句；最后一句按 E 关闭。对话框开着时玩家不能移动。按 Tab 弹出笔记面板，显示"（还没有发现任何线索）"，再按 Tab 关闭。

- [ ] **步骤 6：** 删掉步骤 5 的临时测试行。

- [ ] **提交：**

```bash
git add game/ && git commit -m "feat: 全局 UI（打字机对话框/交互提示/线索笔记）"
```

---

## 任务 5：可调查物组件 + 第一条线索

**文件：**
- 创建：`game/scripts/interactable.gd`
- 修改：`game/scenes/village.tscn`（加村牌；给"自己家"加临时可调查物验证线索流程）

- [ ] **步骤 1：** 创建 `game/scripts/interactable.gd`：

```gdscript
extends Area2D
class_name Interactable
## 通用可调查物：玩家靠近按 E 触发对话，可选地登记一条线索。
## 用法：场景里放 Area2D 挂本脚本，在检查器里填 lines / clue_id。

@export_multiline var lines: PackedStringArray = []
## 留空表示纯调查文本，不产生线索；线索 id 必须存在于 GameState.CLUE_TEXTS
@export var clue_id: String = ""
@export var one_shot := false  ## true 则只能调查一次

var _used := false

func _ready() -> void:
	add_to_group("interactable")

func interact() -> void:
	if one_shot and _used:
		return
	_used = true
	Ui.show_dialogue(lines)
	await Ui.dialogue_finished
	if clue_id != "" and clue_id not in GameState.clues:
		var is_first: bool = GameState.clues.is_empty()
		GameState.add_clue(clue_id)
		if is_first:
			Ui.show_dialogue(["（线索已记入笔记。按 Tab 可随时查看。）"])
```

- [ ] **步骤 2：** 在 `village.tscn` 中加入村牌（位置在村子下方入口处）：

```
Village
└─ Interactables (Node2D)        # 新增容器节点
   └─ VillageSign (Area2D)  ← 挂脚本 interactable.gd，position = (700, 700)
      │   检查器属性：
      │   lines = ["一块旧村牌。「名花流村」四个字被人用刀划得稀烂。",
      │            "上面用红漆重新写了三个字——", "封门村。"]
      │   clue_id = ""（空）
      ├─ V (Polygon2D)  polygon=[(-10,-16),(10,-16),(10,16),(-10,16)]  color=#8a8a7a
      └─ C (CollisionShape2D)  shape=RectangleShape2D size=(28,40)
```

- [ ] **步骤 3：** 同样在 `Interactables` 下加一个临时线索物验证线索流程（任务 7 建好室内后会移走）：

```
   └─ TempClue (Area2D)  ← 挂 interactable.gd，position = (240, 480)（自己家门口）
      │   lines = ["（临时测试）一封没写完的信。"]
      │   clue_id = "home_letter"，one_shot = true
      ├─ V (Polygon2D)  polygon=[(-8,-8),(8,-8),(8,8),(-8,8)]  color=#c9b458
      └─ C (CollisionShape2D)  shape=RectangleShape2D size=(20,20)
```

- [ ] **验证：** F5 运行：走近村牌出现"E 调查"提示，按 E 弹出三句文案；走开提示消失。调查黄色方块后，按 Tab 打开笔记能看到"家里的饭桌……"那条线索；再按 E 调查黄色方块无反应（one_shot 生效）。

- [ ] **提交：**

```bash
git add game/ && git commit -m "feat: 可调查物组件与线索登记流程"
```

---

## 任务 6：门与场景切换 + 5 个灰盒室内

**文件：**
- 创建：`game/scripts/door.gd`、`game/scripts/interior.gd`
- 创建：`game/scenes/interiors/home.tscn`、`chen_home.tscn`、`zhang_home.tscn`、`yizhuang.tscn`、`citang.tscn`
- 修改：`game/scenes/village.tscn`（加 5 个门；删 TempClue）

- [ ] **步骤 1：** 创建 `game/scripts/door.gd`：

```gdscript
extends Area2D
## 门：玩家踏入即切换场景。collision_mask 需能检测到玩家（默认层 1 即可）。

@export_file("*.tscn") var target_scene: String
@export var spawn: String = "default"

func _ready() -> void:
	body_entered.connect(_on_body_entered)

func _on_body_entered(body: Node2D) -> void:
	if body.is_in_group("player"):
		Router.change_scene(target_scene, spawn)
```

- [ ] **步骤 2：** 创建 `game/scripts/interior.gd`：

```gdscript
extends Node2D
## 室内通用脚本：摆放玩家出生点 + 按剧情阶段调整色调。

const DAY_COLOR := Color("b8b2c4")
const NIGHT_COLOR := Color("46405e")

@onready var player: CharacterBody2D = $Player
@onready var tint: CanvasModulate = $Tint
@onready var spawn_points: Node2D = $SpawnPoints

func _ready() -> void:
	var point: Node2D = spawn_points.get_node_or_null(NodePath(Router.spawn_name))
	if point == null:
		point = spawn_points.get_node("default")
	player.global_position = point.global_position
	tint.color = NIGHT_COLOR if GameState.phase == GameState.Phase.NIGHT else DAY_COLOR
```

- [ ] **步骤 3：** 创建室内场景模板。5 个室内结构完全相同，仅"线索物"和"返回出生点"不同。以 `game/scenes/interiors/home.tscn`（自己家）为例：

```
Home (Node2D)  ← 挂脚本 res://scripts/interior.gd
├─ Tint (CanvasModulate)
├─ Floor (Polygon2D)   polygon=[(80,40),(560,40),(560,320),(80,320)]  color=#5a4f45
├─ Walls (StaticBody2D)    # 房间四壁，门口留缺
│  ├─ N (CollisionShape2D)  size=(480,16)  position=(320,48)
│  ├─ W (CollisionShape2D)  size=(16,280)  position=(88,180)
│  ├─ E (CollisionShape2D)  size=(16,280)  position=(552,180)
│  ├─ S1 (CollisionShape2D) size=(200,16)  position=(180,312)   # 门口左侧
│  └─ S2 (CollisionShape2D) size=(200,16)  position=(460,312)   # 门口右侧
├─ ExitDoor (Area2D)  ← 挂 door.gd，position=(320,336)
│  │   target_scene = "res://scenes/village.tscn"，spawn = "from_home"
│  └─ C (CollisionShape2D)  shape=RectangleShape2D size=(80,16)
├─ ClueObject (Area2D)  ← 挂 interactable.gd，position=(320,120)
│  │   lines 与 clue_id 见下表（文案全文在 docs/07-剧情文案.md，灰盒阶段先用下表的 id 行占位即可）
│  │   one_shot = false（线索物可反复看，线索只记一次）
│  ├─ V (Polygon2D)  polygon=[(-10,-10),(10,-10),(10,10),(-10,10)]  color=#c9b458
│  └─ C (CollisionShape2D)  shape=RectangleShape2D size=(24,24)
├─ SpawnPoints (Node2D)
│  └─ default (Marker2D)  position=(320,290)
└─ Player (player.tscn 实例)
```

其余 4 个场景照此复制，差异表：

| 场景文件 | 根节点名 | ExitDoor.spawn | ClueObject.clue_id |
|---|---|---|---|
| `home.tscn` | Home | from_home | home_letter |
| `chen_home.tscn` | ChenHome | from_chen | chen_shroud |
| `zhang_home.tscn` | ZhangHome | from_zhang | zhang_wedding |
| `yizhuang.tscn` | Yizhuang | from_yizhuang | yizhuang_coffin |
| `citang.tscn` | Citang | from_citang | citang_roster |

灰盒阶段每个 ClueObject 的 lines 先填一句占位，如 `["（占位）调查后获得线索 home_letter"]`，正式文案在阶段 3 从 07 号文档填入。

- [ ] **步骤 4：** 在 `village.tscn` 加 5 个门（放在 `Houses` 同级新建 `Doors (Node2D)` 下），每个门是挂 `door.gd` 的 Area2D + RectangleShape2D size=(48,16)：

| 节点名 | position | target_scene | spawn |
|---|---|---|---|
| HomeDoor | (240, 432) | res://scenes/interiors/home.tscn | default |
| ChenDoor | (520, 352) | res://scenes/interiors/chen_home.tscn | default |
| ZhangDoor | (840, 432) | res://scenes/interiors/zhang_home.tscn | default |
| YizhuangDoor | (1060, 592) | res://scenes/interiors/yizhuang.tscn | default |
| CitangDoor | (640, 192) | res://scenes/interiors/citang.tscn | default |

（门贴在房块下沿外侧；对应的 `from_*` 出生点在门外约 40px 处，玩家出来不会立刻再踩到门。）

- [ ] **步骤 5：** 删除任务 5 的 `TempClue` 节点。

- [ ] **验证：** F5 运行：走到任一房门口自动进入室内；室内能调查黄色方块得到线索；从门口缺口走出回到村庄，且出现在对应房子门外（不会瞬间又进门）。5 栋全部测一遍，Tab 笔记里 5 条线索齐全。**注意：集齐 5 条后画面会变暗（夜晚 phase 已自动触发），这是正常的，夜晚演出任务 8 做。**

- [ ] **提交：**

```bash
git add game/ && git commit -m "feat: 门与场景切换、5 个灰盒室内、全部线索可收集"
```

---

## 任务 7：开场字卡

**文件：**
- 创建：`game/scripts/opening.gd`、`game/scenes/opening.tscn`
- 修改：`game/project.godot`（主场景改为 opening）

- [ ] **步骤 1：** 创建 `game/scripts/opening.gd`（字卡全文与 docs/07-剧情文案.md 同步）：

```gdscript
extends Control
## 开场字卡：黑屏逐张显示旁白，按 E/空格推进。播完进村庄。

const CARDS := [
	"一九九五年，六月初六。",
	"我叫名瞳，名花流村人。\n那一天，我跟着师傅的送葬队上山。",
	"师傅死得蹊跷。寿衣的针脚里，缝着我看不懂的符。",
	"我没能走下那座山。\n他们把我钉进了我爹的棺材，埋进了棺材地。",
	"棺材里有一本书。书上的字，是金色的。\n四年，我读完了它。",
	"第四年的雨夜，我引下了那道雷。",
	"轰——",
	"公元两千年。我回来了。",
]

@onready var label: RichTextLabel = $Center/CardLabel

var _index := -1
var _tween: Tween

func _ready() -> void:
	GameState.reset()
	_next_card()

func _unhandled_input(event: InputEvent) -> void:
	if event.is_action_pressed("interact") or event.is_action_pressed("ui_accept"):
		if _tween and _tween.is_running():
			_tween.kill()
			label.modulate.a = 1.0
		else:
			_next_card()

func _next_card() -> void:
	_index += 1
	if _index >= CARDS.size():
		Router.change_scene("res://scenes/village.tscn", "wake_up")
		return
	label.text = CARDS[_index]
	label.modulate.a = 0.0
	_tween = create_tween()
	_tween.tween_property(label, "modulate:a", 1.0, 1.2)
```

- [ ] **步骤 2：** 创建场景 `game/scenes/opening.tscn`：

```
Opening (Control)  ← 挂脚本 res://scripts/opening.gd
│     anchor 全屏（Full Rect 预设）
├─ Bg (ColorRect)   全屏，color = 纯黑 (0,0,0,1)
└─ Center (CenterContainer)   全屏
   └─ CardLabel (RichTextLabel)
         fit_content = true，custom_minimum_size = (480, 0)
         horizontal_alignment = Center
```

- [ ] **步骤 3：** `project.godot` 中主场景改为：

```ini
run/main_scene="res://scenes/opening.tscn"
```

- [ ] **验证：** F5 运行：黑屏上字卡逐张淡入，按 E 推进（淡入中按 E 立刻显示全）；8 张播完自动进村，且玩家出生在村子最下方的"棺材地"位置（不是村中心）。

- [ ] **提交：**

```bash
git add game/ && git commit -m "feat: 开场字卡序列"
```

---

## 任务 8：夜幕过场演出（灰盒版）

**文件：**
- 修改：`game/scripts/village.gd`
- 修改：`game/scenes/village.tscn`（加 NightGhost 节点）

- [ ] **步骤 1：** `village.tscn` 中加一个远处白影节点（祠堂旁）：

```
Village
└─ NightGhost (Polygon2D)
      polygon = [(-7,-14),(7,-14),(7,14),(-7,14)]，color = (1,1,1,0.85)
      position = (760, 120)，visible = false
```

- [ ] **步骤 2：** 替换 `game/scripts/village.gd` 为：

```gdscript
extends Node2D
## 村庄外景：摆放玩家、昼夜色调、夜幕过场。

const DAY_COLOR := Color("9b95ad")
const NIGHT_COLOR := Color("3a3354")

@onready var player: CharacterBody2D = $Player
@onready var tint: CanvasModulate = $Tint
@onready var spawn_points: Node2D = $SpawnPoints
@onready var night_ghost: Polygon2D = $NightGhost

func _ready() -> void:
	_place_player()
	night_ghost.visible = GameState.has_flag("night_cutscene_done")
	tint.color = NIGHT_COLOR if GameState.phase == GameState.Phase.NIGHT else DAY_COLOR
	if GameState.phase == GameState.Phase.NIGHT \
			and not GameState.has_flag("night_cutscene_done"):
		_night_cutscene()

func _place_player() -> void:
	var point: Node2D = spawn_points.get_node_or_null(NodePath(Router.spawn_name))
	if point == null:
		point = spawn_points.get_node("default")
	player.global_position = point.global_position

func _night_cutscene() -> void:
	GameState.set_flag("night_cutscene_done")
	player.can_move = false
	tint.color = DAY_COLOR
	var t := create_tween()
	t.tween_property(tint, "color", NIGHT_COLOR, 3.0)
	await t.finished
	Ui.show_dialogue([
		"天，毫无征兆地黑了下来。",
		"这不是日落。日落没有这么快，也没有这么静。",
		"远处，祠堂的方向，立着一点白。",
		"——去祠堂。",
	])
	await Ui.dialogue_finished
	night_ghost.visible = true
	player.can_move = true
```

- [ ] **验证：** F5 从头玩：收集线索时室内外都是白天色调；**在室内拿到第 5 条线索后走出室外**，天色在 3 秒内变暗 → 弹出四句旁白 → 祠堂边出现白影 → 恢复操作。再进出其他房子，夜色保持，过场不重播。

- [ ] **提交：**

```bash
git add game/ && git commit -m "feat: 夜幕过场演出（灰盒）"
```

---

## 任务 9：结局——祠堂瞎子对话 + 结束字卡

**文件：**
- 创建：`game/scripts/xiazi.gd`、`game/scripts/ending.gd`、`game/scenes/ending.tscn`
- 修改：`game/scenes/interiors/citang.tscn`（加瞎子）；`game/scripts/interior.gd` 不动

- [ ] **步骤 1：** 创建 `game/scripts/xiazi.gd`：

```gdscript
extends Area2D
## 瞎子：夜晚出现在祠堂。对话结束后进入结局字卡。

const LINES := [
	"瞎子：「回来了？」",
	"他坐在名册前，空洞的眼窝正对着我，像是早就等在这里。",
	"瞎子：「四年了。书，读完了吧。」",
	"我有一万句话想问。爹娘在哪，全村人去了哪，谁害的师傅——",
	"瞎子：「都会有答案。但你先得弄明白一件事。」",
	"瞎子：「你从那口棺材里爬出来的时候——」",
	"瞎子：「就已经不算活人了。」",
]

func _ready() -> void:
	add_to_group("interactable")
	var is_night: bool = GameState.phase == GameState.Phase.NIGHT
	visible = is_night
	monitorable = is_night

func interact() -> void:
	Ui.show_dialogue(LINES)
	await Ui.dialogue_finished
	GameState.set_phase(GameState.Phase.ENDING)
	Router.change_scene("res://scenes/ending.tscn")
```

- [ ] **步骤 2：** 在 `citang.tscn` 中加节点：

```
Citang
└─ XiaZi (Area2D)  ← 挂脚本 res://scripts/xiazi.gd，position = (320, 100)
   ├─ V (Polygon2D)  polygon=[(-8,-12),(8,-12),(8,12),(-8,12)]  color=#222222
   └─ C (CollisionShape2D)  shape=RectangleShape2D size=(20,28)
```

- [ ] **步骤 3：** 创建 `game/scripts/ending.gd`：

```gdscript
extends Control
## 结束字卡。最后一张按 E 回到开场（并重置状态）。

const CARDS := [
	"封门村的夜，才刚刚开始。",
	"《灵棺夜行 · 第一夜》\n\n—— 完 ——",
	"改编自小说《灵棺夜行》\n\n按 E 重新开始",
]

@onready var label: RichTextLabel = $Center/CardLabel

var _index := -1
var _tween: Tween

func _ready() -> void:
	_next_card()

func _unhandled_input(event: InputEvent) -> void:
	if event.is_action_pressed("interact") or event.is_action_pressed("ui_accept"):
		if _tween and _tween.is_running():
			_tween.kill()
			label.modulate.a = 1.0
		elif _index >= CARDS.size() - 1:
			Router.change_scene("res://scenes/opening.tscn")
		else:
			_next_card()

func _next_card() -> void:
	_index += 1
	label.text = CARDS[_index]
	label.modulate.a = 0.0
	_tween = create_tween()
	_tween.tween_property(label, "modulate:a", 1.0, 1.2)
```

- [ ] **步骤 4：** 创建场景 `game/scenes/ending.tscn`，结构与 `opening.tscn` 完全相同（Control 全屏 + 黑色 Bg + Center/CardLabel），仅挂的脚本换成 `res://scripts/ending.gd`。

- [ ] **验证：** F5 从头玩：白天进祠堂**看不到**瞎子（黑色方块不可见也不可交互）；夜幕过场后进祠堂，瞎子出现，对话 7 句播完自动进入结束字卡；最后一张按 E 回到开场字卡，线索清零（Tab 验证）。

- [ ] **提交：**

```bash
git add game/ && git commit -m "feat: 祠堂结局对话与结束字卡，流程闭环"
```

---

## 任务 10：阶段验收——全流程通关测试

- [ ] 从 F5 开始不碰编辑器，完整走一遍并核对：

1. 开场 8 张字卡可推进、可跳过淡入
2. 醒来位置在棺材地，能一路走到村口调查村牌
3. 5 栋房子全部能进出，出生点位置正确，无"门口循环传送"
4. 5 条线索全部可收集，Tab 笔记逐条正确显示，重复调查不重复记录
5. 第 5 条线索 → 出村触发夜幕过场（变暗 + 旁白 + 白影）
6. 夜晚进祠堂 → 瞎子对话 → 结束字卡 → 按 E 重开，状态清零
7. 全程 Godot 输出面板无红色报错（黄色警告可忽略）

- [ ] 任一项不符 → 修复后重测全流程，直到 7 项全过
- [ ] **提交（如有修复）：**

```bash
git add game/ && git commit -m "fix: 灰盒全流程验收修复"
```

- [ ] 更新 `PROJECT_STATUS.md`：灰盒原型状态改为"完成"，随本次或下次 commit 提交

**阶段 1 完成标志达成：游戏能从头玩到尾。接下来进入 docs/04（垂直切片）。**
